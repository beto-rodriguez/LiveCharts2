<Project>

  <PropertyGroup Condition="'$(Skia)' == ''">
    <SkiaRenderer>true</SkiaRenderer>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Skia)' == 'true'">
    <UnoFeatures>$(UnoFeatures);SkiaRenderer</UnoFeatures>
  </PropertyGroup>

</Project>
