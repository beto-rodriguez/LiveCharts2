<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiSample.Lines.Padding.View"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    xmlns:vms="clr-namespace:ViewModelsSamples.Lines.Padding;assembly=ViewModelsSamples">

    <ContentPage.BindingContext>
        <vms:ViewModel/>
    </ContentPage.BindingContext>

    <lvc:CartesianChart
        DrawMarginFrame="{lvc:Frame
            Stroke={lvc:SolidColorPaint Color='64b4b4b4'},
            Fill={lvc:SolidColorPaint Color='32dcdcdc'}}">
        <lvc:CartesianChart.Series>

            <!--
            this series fits the draw margin area
            the key is to set the DataPadding to 0,0
            also remove GeometryStroke, GeometryFill and GeometrySize
            to prevent the series from reserving a space for the series geometry.
            -->

            <lvc:XamlLineSeries
                Values="2, 1, 3, 5, 3, 4, 6"
                GeometryStroke="{Null}"
                GeometryFill="{Null}"
                DataPadding="0,0"/>

        </lvc:CartesianChart.Series>
    </lvc:CartesianChart>
</ContentPage>
