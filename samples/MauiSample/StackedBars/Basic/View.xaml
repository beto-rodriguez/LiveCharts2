<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiSample.StackedBars.Basic.View"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    xmlns:vms="clr-namespace:ViewModelsSamples.StackedBars.Basic;assembly=ViewModelsSamples"
    x:DataType="vms:ViewModel">

    <ContentPage.BindingContext>
        <vms:ViewModel/>
    </ContentPage.BindingContext>

    <lvc:CartesianChart>
        <lvc:CartesianChart.Series>
            <lvc:SeriesCollection>
                <lvc:XamlStackedColumnSeries
                    Values="{Binding Values1}"
                    ShowDataLabels="True"
                    YToolTipLabelFormatter="{Binding Formatter}"/>
                <lvc:XamlStackedColumnSeries
                    Values="{Binding Values2}"
                    ShowDataLabels="True"
                    YToolTipLabelFormatter="{Binding Formatter}"/>
                <lvc:XamlStackedColumnSeries
                    Values="{Binding Values3}"
                    ShowDataLabels="True"
                    YToolTipLabelFormatter="{Binding Formatter}"/>
            </lvc:SeriesCollection>
        </lvc:CartesianChart.Series>
    </lvc:CartesianChart>
</ContentPage>
