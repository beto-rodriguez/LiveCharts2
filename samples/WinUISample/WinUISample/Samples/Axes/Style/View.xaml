<UserControl
    x:Class="WinUISample.Axes.Style.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:vms="using:ViewModelsSamples.Axes.Style"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <vms:ViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <x:String x:Key="gray">#c3c3c3</x:String>
        <x:String x:Key="gray1">#a0a0a0</x:String>
        <x:String x:Key="gray2">#5a5a5a</x:String>
        <x:String x:Key="gray3">#3c3c3c</x:String>
    </UserControl.Resources>

    <lvc:CartesianChart
        Background="#303030"
        Margin="20"
            
        ZoomMode="Both"
        TooltipPosition="Hidden">

        <!--
        Nested extensions are not working in Uno
        DrawMarginFrame="{lvc:Frame Stroke={lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=2}}"
        -->

        <lvc:CartesianChart.Series>
            <lvc:XamlLineSeries
                Values="{Binding Values}"
                Stroke="{lvc:SolidColorPaint Color='#2196F3', StrokeWidth=4}"
                Fill="{x:Null}"
                GeometryFill="{x:Null}"
                GeometryStroke="{x:Null}"/>
        </lvc:CartesianChart.Series>

        <lvc:CartesianChart.XAxes>
            <lvc:XamlAxis
                AxisName="X Axis"
                NamePaint="{lvc:SolidColorPaint Color={StaticResource gray1}}"
                TextSize="18"
                LabelsPaint="{lvc:SolidColorPaint Color={StaticResource gray}}"
                    
                SubseparatorsPaint="{lvc:SolidColorPaint Color={StaticResource gray2}, StrokeWidth=0.5}"
                SubseparatorsCount="9"
                ZeroPaint="{lvc:SolidColorPaint Color={StaticResource gray1}, StrokeWidth=2}"
                TicksPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1.5}"
                SubticksPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1}"/>

            <!--
            Nested extensions are not working in Uno
            SeparatorsPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1, PathEffect={lvc:Dashed Array='3,3'}}"
            -->

        </lvc:CartesianChart.XAxes>

        <lvc:CartesianChart.YAxes>
            <lvc:XamlAxis
                AxisName="Y Axis"
                NamePaint="{lvc:SolidColorPaint Color={StaticResource gray1}}"
                TextSize="18"
                LabelsPaint="{lvc:SolidColorPaint Color={StaticResource gray}}"
                    
                SubseparatorsPaint="{lvc:SolidColorPaint Color={StaticResource gray2}, StrokeWidth=0.5}"
                SubseparatorsCount="9"
                ZeroPaint="{lvc:SolidColorPaint Color={StaticResource gray1}, StrokeWidth=2}"
                TicksPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1.5}"
                SubticksPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1}"/>

            <!--
            Nested extensions are not working in Uno
            SeparatorsPaint="{lvc:SolidColorPaint Color={StaticResource gray}, StrokeWidth=1, PathEffect={lvc:Dashed Array='3,3'}}"
            -->

        </lvc:CartesianChart.YAxes>

    </lvc:CartesianChart>
</UserControl>
