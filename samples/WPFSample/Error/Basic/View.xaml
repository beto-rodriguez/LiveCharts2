<UserControl
    x:Class="WPFSample.Error.Basic.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:vms="clr-namespace:ViewModelsSamples.Error.Basic;assembly=ViewModelsSamples">

    <UserControl.DataContext>
        <vms:ViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <lvc:CartesianChart Grid.Row="0">
            <lvc:CartesianChart.Series>
                <lvc:SeriesCollection>
                    <lvc:XamlColumnSeries
                        Values="{Binding Values1}"
                        ShowError="True"
                        Padding="0"/>
                    <lvc:XamlColumnSeries
                        Values="{Binding Values2}"
                        ShowError="True"
                        Padding="0"/>
                </lvc:SeriesCollection>
            </lvc:CartesianChart.Series>
        </lvc:CartesianChart>

        <lvc:CartesianChart Grid.Row="1">
            <lvc:CartesianChart.Series>
                <lvc:SeriesCollection>
                    <lvc:XamlLineSeries
                        Values="{Binding Values1}"
                        ShowError="True"
                        GeometrySize="4"
                        Fill="{x:Null}"/>
                </lvc:SeriesCollection>
            </lvc:CartesianChart.Series>
        </lvc:CartesianChart>

        <lvc:CartesianChart Grid.Row="2">
            <lvc:CartesianChart.Series>
                <lvc:SeriesCollection>
                    <lvc:XamlScatterSeries
                        Values="{Binding Values3}"
                        ShowError="True"
                        GeometrySize="10"/>
                </lvc:SeriesCollection>
            </lvc:CartesianChart.Series>
            <lvc:CartesianChart.XAxes>
                <!--
                To get more help about DateTime axes see:
                https://livecharts.dev/docs/wpf/latest/samples.axes.dateTimeScaled
                -->
                <lvc:AxesCollection>
                    <lvc:XamlDateTimeAxis
                        Interval="1.00:00:00"
                        DateFormatter="{Binding Formatter}"/>
                </lvc:AxesCollection>
            </lvc:CartesianChart.XAxes>
        </lvc:CartesianChart>
    </Grid>

</UserControl>
