<Window x:Class="WPFSample.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSample"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Window.DataContext>
        <local:ViewModel1></local:ViewModel1>
    </Window.DataContext>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" MinHeight="450"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="800" />
        </Grid.ColumnDefinitions>
        <Button Click="Button_Click">AddSeries</Button>
        <Button Click="Button_Click_1" Grid.Row="1">RemoveSeries</Button>
        <lvc:CartesianChart Grid.Row="2" x:Name="cc"
                            Series="{Binding OSeries}"
                            YAxes="{Binding OYAxes}"
                            XAxes="{Binding OXAxes}"
                            ZoomMode="X"
                            LegendPosition="Top">
            
        </lvc:CartesianChart>
    </Grid>
</Window>
