<UserControl
    x:Class="WPFSample.General.Legends.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:vms="clr-namespace:ViewModelsSamples.General.Legends;assembly=ViewModelsSamples">

    <UserControl.DataContext>
        <vms:ViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox
            Grid.Row="0"
            ItemsSource="{Binding Positions}"
            SelectedItem="{Binding SelectedPosition}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <lvc:CartesianChart
            Grid.Row="1"
            LegendPosition="{Binding SelectedPosition}"
            SeriesSource="{Binding Data}">
            <lvc:CartesianChart.SeriesTemplate>
                <DataTemplate DataType="{x:Type vms:ChartData}">
                    <lvc:XamlColumnSeries SeriesName="{Binding Name}" Values="{Binding Values}"/>
                </DataTemplate>
            </lvc:CartesianChart.SeriesTemplate>
        </lvc:CartesianChart>
    </Grid>

</UserControl>
