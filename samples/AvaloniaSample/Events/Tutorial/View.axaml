<UserControl
    x:Class="AvaloniaSample.Events.Tutorial.View"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
    xmlns:vms="using:ViewModelsSamples.Events.Tutorial"
    x:DataType="vms:ViewModel">

  <UserControl.DataContext>
    <vms:ViewModel/>
  </UserControl.DataContext>

  <lvc:CartesianChart
      FindingStrategy="{Binding Strategy}"
      PointerPressedCommand="{Binding PressedCommand}"
      HoveredPointsChangedCommand="{Binding HoveredPointsChangedCommand}">
    <lvc:CartesianChart.Series>
      <lvc:XamlColumnSeries Values="{Binding Values1}" Stroke="{lvc:SolidColorPaint Color='#00000000'}"/>
      <lvc:XamlColumnSeries Values="{Binding Values2}" Stroke="{lvc:SolidColorPaint Color='#00000000'}"/>
    </lvc:CartesianChart.Series>
  </lvc:CartesianChart>

</UserControl>
